<nav class="navbar navbar-expand-lg navbar-dark animate__animated animate__fadeInDown">
  <div class="container">
    <a class="navbar-brand" href="/">MJ TRIBUTE CONCERTS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto">
        <li class="nav-item"><a class="nav-link" href="/">HOME</a></li>
        <li class="nav-item"><a class="nav-link" href="/auth/login">USER AUTHENTICATION</a></li>
        <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
        <li class="nav-item"><a class="nav-link" href="/events">EVENT MANAGEMENT</a></li>
        <li class="nav-item"><a class="nav-link" href="/admin/enquiries">ENQUIRIES</a></li>
        <% } %>
        <li class="nav-item"><a class="nav-link" href="/dashboard">BOOKING & DASHBOARD</a></li>
        <li class="nav-item"><a class="nav-link" href="/contact">CONTACT</a></li>
      </ul>
      <div class="navbar-text text-white ml-auto">
        <% if (typeof user !== 'undefined' && user) { %>
          <div class="user-info">
            <strong><%= user.name %></strong><br>
            <small><%= user.email %></small>
          </div>
        <% } else { %> 
          <small>Guest</small>
        <% } %>
      </div>
    </div>
  </div>
</nav>

